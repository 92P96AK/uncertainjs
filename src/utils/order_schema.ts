import { RelationalSchema } from "../interface";

export const GetOrderedSchema = (schema: RelationalSchema): RelationalSchema => {
  const orderedSchema: RelationalSchema = {};
//   later
  return orderedSchema;
};
